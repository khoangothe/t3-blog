import { Container, Section } from "@radix-ui/themes";

export default async function Page() {
  return (
    <Section className="px-auto mx-auto flex min-h-screen max-w-xl">
      <Container>About</Container>
    </Section>
  );
}
